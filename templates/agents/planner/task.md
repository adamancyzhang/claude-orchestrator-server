## Task to Execute

**Title**: {{task_title}}
**Description**: {{task_description}}
**Acceptance Criteria**: {{task_criteria}}

## Origin
The user's original requirement is at `{{original_requirement_path}}`. Read it first whenever the task description feels under-specified — the authoritative intent lives there.

## Intent
Produce a self-contained design document — architecture, interfaces, data flow, concrete Execute steps with verifiable acceptance criteria — so each downstream link can ground its work in this one file.

## Required Output Files

- `result_path`: `{{result_path}}`
- `local_doc_path`: `{{local_doc_path}}`

Write to both paths. After writing, use the **Read** tool on `result_path` to confirm the file exists and is non-empty.

{{retry_hint}}
