## Commit Task

Changed files:
{{changed_files}}

Untracked files:
{{untracked_files}}

Task: {{task_title}} ({{link}})

Generate a concise git commit message (single line, under 72 chars).
Output ONLY the commit message.
